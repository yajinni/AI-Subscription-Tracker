# Temporary branch-only executor; remove before merge.
name: Run authentication race-safety patch

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/run-tighten-cancel-races.yml

permissions:
  contents: write

jobs:
  patch-and-validate:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Extract and run race-safety patch
        shell: bash
        run: |
          awk '
            /python3 <<'\''PY'\''/ { capture=1; next }
            capture && /^          PY$/ { capture=0; exit }
            capture { sub(/^          /, ""); print }
          ' .github/workflows/tighten-cancel-races.yml > /tmp/tighten.py
          test -s /tmp/tighten.py
          python3 /tmp/tighten.py
          cargo fmt --manifest-path src-tauri/Cargo.toml
          cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
      - name: Install frontend dependencies
        run: npm install
      - name: Validate frontend
        run: |
          npm run check
          npm run build
      - name: Commit race-safety changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src-tauri/src/grok_login.rs src-tauri/src/google_ai_studio_oauth.rs src-tauri/src/lib.rs src-tauri/src/oauth.rs src-tauri/src/opencode_login.rs
          git commit -m "Prevent stale authentication completion after cancel"
          git push origin HEAD:${{ github.head_ref }}
